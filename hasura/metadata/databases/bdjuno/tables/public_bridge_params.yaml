table:
  name: bridge_params
  schema: public
select_permissions:
  - permission:
      allow_aggregations: false
      columns:
        - id
        - module_admin
        - parties
        - tss_threshold
      filter: {}
      limit: 100
    role: anonymous