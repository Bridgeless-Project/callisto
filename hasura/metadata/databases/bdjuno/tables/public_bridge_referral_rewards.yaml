table:
  name: bridge_referral_rewards
  schema: public
select_permissions:
- permission:
    allow_aggregations: false
    columns:
    - referral_id
    - token_id
    - to_claim
    - total_claimed_amount
    filter: {}
    limit: 100
  role: anonymous
