table:
  name: bridge_params
  schema: public
select_permissions:
  - permission:
      allow_aggregations: false
      columns:
        - id
        - module_admin
        - parties
        - tss_threshold
        - relayer_account
        - relayer_accounts
        - epoch_id
        - supporting_time
      filter: {}
      limit: 100
    role: anonymous
