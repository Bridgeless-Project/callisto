table:
  name: nfts
  schema: public
select_permissions:
- permission:
    allow_aggregations: false
    columns:
    - address
    - owner
    - locked_amount
    - available_amount
    - delegations
    filter: {}
    limit: 100
  role: anonymous
